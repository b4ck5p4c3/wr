{
  "name": "wr-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "docs": "bun gen-docs.js",
    "build": "bun gen-docs.js && vite build",
    "preview": "vite preview",
    "format": "prettier --write src"
  },
  "dependencies": {
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "prettier": "^3.3.0",
    "vite": "^5.4.0",
    "yaml": "^2.5.0"
  }
}
